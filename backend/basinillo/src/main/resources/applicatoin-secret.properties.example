# ===================================================================
# DATABASE CONNECTION (Supabase PostgreSQL)
# ===================================================================

# 1. The Connection URL
# Replace [YOUR-DB-PASSWORD] with your actual database password
# Note: Port 5432 is for direct connections. Port 6543 is for the transaction pooler (better for serverless).
spring.datasource.url=jdbc:postgresql://db.flwblgkivkrrxsuhxigt.supabase.co:5432/postgres

# 2. The Database Username
# usually just 'postgres' or 'postgres.your-project-ref'
spring.datasource.username=postgres

# 3. The Database Password
# CRITICAL: This is NOT the 'sb_publishable' key. 
# It is the password you typed when you created the project.
spring.datasource.password=[YOUR_ACTUAL_DB_PASSWORD]

# ===================================================================
# JPA / HIBERNATE SETTINGS
# ===================================================================

# Tells Hibernate we are using PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Automatically updates the database schema when you change your Java entities
# Use 'update' for development, 'validate' for production
spring.jpa.hibernate.ddl-auto=update

# Shows the SQL queries in the console (Helpful for debugging)
spring.jpa.show-sql=true

# Formats the SQL so it's easier to read
spring.jpa.properties.hibernate.format_sql=true