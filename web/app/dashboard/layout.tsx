/**
 * Dashboard Layout — passthrough layout.
 * The DashboardLayout component (used in page.tsx) handles the full shell.
 */
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
